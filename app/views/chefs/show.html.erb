<%= render 'shared/page_title', title: "Chef #{@chef.chefname}'s Profile" %>

<div class="col-md-12">
	<div align="center">
		<div class="col-md-4 col-md-offset-4 center">
			<%= gravatar_for @chef, size: 150 %>
		</div>
	</div>
</div>

<% if @chef.recipes.count != 0 %>
	<div class="recipe-title">
		<h2 align="center"><%= @chef.chefname %>'s recipes</h2>
		<br />
		<div class="center">
			<%= will_paginate @chef_recipes %>
		</div>
		<%= render 'recipes/recipe', obj: @chef_recipes %>
		<div class="center">
			<%= will_paginate @chef_recipes %>
		</div>
	</div>
<% else %>
	<div class="recipe-title">
		<h2 align="center"><%= @chef.chefname %> has no recipes yet</h2>
	</div>
<% end %>